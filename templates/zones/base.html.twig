<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com?plugins=forms,typography"></script>
    <style>
        /* Animation d'apparition de la modale */
        .modal-enter {
            opacity: 0;
            transform: scale(0.95);
            transition: opacity 0.3s ease, transform 0.3s ease;
        }
        .modal-enter-active {
            opacity: 1;
            transform: scale(1);
        }
    </style>
</head>
<body class="{{ body_class|default('bg-gray-800') }}">

<!-- Navigation -->
<nav class="absolute top-0 right-0 p-6 z-50">
    <ul class="flex space-x-4">
        <li><a href="{{ path('app_home') }}" class="nav-link">Accueil</a></li>
        <li><a href="{{ path('app_zones') }}" class="nav-link">Nos zones</a></li>
        <li><a href="{{ path('app_services_veterinaire') }}" class="nav-link">Services vétérinaires</a></li>
        <li><a href="{{ path('app_services') }}" class="nav-link">Services du zoo</a></li>
        <li><a href="{{ path('app_login') }}" class="nav-link">Connexion</a></li>
        <li><a href="{{ path('admin_dashboard') }}" class="nav-link">Admin</a></li>
        <li><a href="{{ path('app_contact') }}" class="nav-link">Avis et Contact</a></li>
    </ul>
</nav>

<!-- Contenu de la page -->
<div class="container mx-auto py-24 text-center">
    {% block content %}{% endblock %}
</div>

</body>
</html>
